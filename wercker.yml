box: wercker/ubuntu

build:
    steps:
    - script:
        name: Update apt
        code: |
            sudo apt-get update
    - script:
        name: Install latex CJK
        code: |
            sudo apt-get install -y latex-cjk-all
    - script:
        name: compile pdf
        code: |
            cd library
            make pdf
