## 遅延評価SegmentTree(総和) {#LasySumSegmentTree}
SegmentTreeでは区間に対する更新に $O(n \; log\; n)$ かかってしまう.

### 計算量 {#complecity .unnumbered}
+ add(s,t): O(log n) ※範囲の値の更新
+ get(s,t): O(log n) ※範囲の値の取得

### Source Code {.unnumbered}

~~~~{LazySumSegmentTree.java}
~~~~

\withoutTeXbegin

### Verified {.unnumbered}
TODO

\withoutTeXend

