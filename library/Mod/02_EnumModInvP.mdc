##逆元列挙 (mod p) {#EnumInvModP}
$mod \: p$ の逆元は1からNまで$O(N)$で計算できる.

### 計算量 {#complecity .unnumbered}
$O(N)$ ※列挙する最大の数

### Source Code {.unnumbered}

~~~~{.java}
//long[] inv = new long[MAX]; //1-indexed
public static void mod_inv(long[] inv, long p){
	inv[1] = 1;
	for(int i = 2; i <= N; i++){
		inv[i] = p - (p / i) * inv[p % i] % p;
	}
}
~~~~

### Verified {.unnumbered}
する予定

